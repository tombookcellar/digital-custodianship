Place the PDFs here, named to match `hugo.toml`:

- `digital-custodianship.pdf`
- `asset-custodianship.pdf`

Update the filenames/titles in `hugo.toml` under `[[params.pdfs]]` if you'd rather use different names.
